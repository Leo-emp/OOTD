import type { Metadata } from "next";

export const metadata: Metadata = { title: "Seasonal Wardrobe Refresh" };

export default function SeasonalRefreshLayout({ children }: { children: React.ReactNode }) {
  return children;
}

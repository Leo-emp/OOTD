// SEO metadata for genre explorer pages
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Genre Explorer",
};

export default function GenreLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Style Evolution" };

export default function StyleEvolutionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
